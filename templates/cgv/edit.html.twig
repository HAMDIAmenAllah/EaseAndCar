{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Edit CGV{% endblock %}

{% block content %}

<div class="flex-column container mt-5">
    <h1>Modifier la page CGV</h1>
    {{ form_start(form) }}
        {{ form_widget(form.Contenu) }}
        <button class="btn btn-ease mt-2">{{ button_label|default('Mettre à jour') }}</button>
    {{ form_end(form) }}
    

    {# {{ include('cgv/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('cgv_index') }}">back to list</a>

    {{ include('cgv/_delete_form.html.twig') }} #}
</div>
{% endblock %}
