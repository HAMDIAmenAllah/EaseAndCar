<form method="post" action="{{ path('sectionnosengagements_delete', {'id': sectionnosengagement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sectionnosengagement.id) }}">
    <button class="btn">Delete</button>
</form>
