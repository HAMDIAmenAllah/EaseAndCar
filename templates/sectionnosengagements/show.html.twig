{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Sectionnosengagements{% endblock %}

{% block content %}
    <div class="flex-column container mt-5">

    <h1>Sectionnosengagements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sectionnosengagement.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ sectionnosengagement.image }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ sectionnosengagement.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ sectionnosengagement.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sectionnosengagements_index') }}">back to list</a>

    <a href="{{ path('sectionnosengagements_edit', {'id': sectionnosengagement.id}) }}">edit</a>

    {{ include('sectionnosengagements/_delete_form.html.twig') }}
    </div>

{% endblock %}
