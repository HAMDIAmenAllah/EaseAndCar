
{% block content %}
<div class="flex-column container">

    {{ include('easeandcar/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {# <a href="{{ path('easeandcar_index') }}">back to list</a>

    {{ include('easeandcar/_delete_form.html.twig') }} #}
</div>
{% endblock %}
