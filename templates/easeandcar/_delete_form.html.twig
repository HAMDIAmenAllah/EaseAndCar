<form method="post" action="{{ path('easeandcar_delete', {'id': easeandcar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ easeandcar.id) }}">
    <button class="btn">Delete</button>
</form>
