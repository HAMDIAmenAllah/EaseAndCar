<form method="post" action="{{ path('devis_delete', {'id': devi.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer ce devis ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devi.id) }}">
    <button class="dropdown-item ">Supprimer</button>
</form>
