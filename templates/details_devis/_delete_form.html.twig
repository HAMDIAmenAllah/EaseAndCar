<form method="post" action="{{ path('detailsdevis_delete', {'id': details_devi.id, 'devis': details_devi.Devis.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ details_devi.id) }}">
        <button class="btn btn-danger" style="margin-left: 129px; margin-top: -66px;">Supprimer</button>
    </form>