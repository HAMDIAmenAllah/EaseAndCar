{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Commentcamarche{% endblock %}

{% block content %}
    <div class="flex-column container mt-5">

    <h1>Commentcamarche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentcamarche.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ commentcamarche.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ commentcamarche.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commentcamarche_index') }}">back to list</a>

    <a href="{{ path('commentcamarche_edit', {'id': commentcamarche.id}) }}">edit</a>

    {{ include('commentcamarche/_delete_form.html.twig') }}
    </div>
{% endblock %}
