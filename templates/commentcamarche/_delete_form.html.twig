<form method="post" action="{{ path('commentcamarche_delete', {'id': commentcamarche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentcamarche.id) }}">
    <button class="btn">Delete</button>
</form>
