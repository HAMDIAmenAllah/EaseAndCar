<form method="post" action="{{ path('bandeau_delete', {'id': bandeau.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bandeau.id) }}">
    <button class="btn">Delete</button>
</form>
