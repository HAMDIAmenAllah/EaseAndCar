{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Edit FAQ{% endblock %}

{% block content %}
<div class="flex-column container mt-5">

    <h1>Modifier la page FAQ</h1>
    {{ form_start(form) }}
        {{ form_widget(form.Contenu) }}
         <button class="btn btn-ease mt-2">{{ button_label|default('Mettre à jour') }}</button>
    {{ form_end(form) }}
    

    {# <a type="button" class="btn btn-ease" href="{{ path('faq_liste') }}">Retour</a> #}
    
    {# {{ include('faq/_delete_form.html.twig') }} #}
</div>
{% endblock %}
