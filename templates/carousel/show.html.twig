{% extends 'base.html.twig' %}

{% block title %}Carousel{% endblock %}

{% block body %}
    <h1>Carousel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carousel.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ carousel.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ carousel.image }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ carousel.contenu }}</td>
            </tr>
            <tr>
                <th>BtnTxt</th>
                <td>{{ carousel.btnTxt }}</td>
            </tr>
            <tr>
                <th>BtnUrl</th>
                <td>{{ carousel.btnUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('carousel_index') }}">back to list</a>

    <a href="{{ path('carousel_edit', {'id': carousel.id}) }}">edit</a>

    {{ include('carousel/_delete_form.html.twig') }}
{% endblock %}
