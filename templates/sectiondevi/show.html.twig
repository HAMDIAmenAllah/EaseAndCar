{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Sectiondevi{% endblock %}

{% block content %}
    <div class="flex-column container mt-5">

    <h1>Sectiondevi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sectiondevi.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ sectiondevi.image }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ sectiondevi.titre }}</td>
            </tr>
            <tr>
                <th>Btn</th>
                <td>{{ sectiondevi.btn }}</td>
            </tr>
            <tr>
                <th>Urlbtn</th>
                <td>{{ sectiondevi.urlbtn }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sectiondevi_index') }}">back to list</a>

    <a href="{{ path('sectiondevi_edit', {'id': sectiondevi.id}) }}">edit</a>

    {{ include('sectiondevi/_delete_form.html.twig') }}
    </div>
{% endblock %}
