<form method="post" action="{{ path('sectionnosservices_delete', {'id': sectionnosservice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sectionnosservice.id) }}">
    <button class="btn">Delete</button>
</form>
