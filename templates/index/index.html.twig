{% extends 'base.html.twig' %}

{% block title %}{{controller_name}}{% endblock %}
{% block stylesheets %}
  <link href="{{ asset('/assets/css/styleHoussem.css') }}" rel="stylesheet">
{% endblock %}
{% block content %}
<header id="header" class="fixed-top d-flex align-items-center ">
    <div class="container d-flex justify-content-between align-items-center">

      <div class="logo">
        <a href="index.html"><img src="{{asset('assets/img/logo_EC.png')}}" alt="Ease & Car"></a>
        <!-- Uncomment below if you prefer to use an image logo -->
        <!-- <a href="index.html"><img src="assets/img/logo.png" alt="" class="img-fluid"></a>-->
      </div>

      <nav id="navbar" class="navbar fs-1">
        <ul>
          <li><a {% if var is same as ('home') %} class="active" {% endif %} href="{{ path('home')}}">ACCUEIL</a></li>
            <li><a {% if var is same as ('about') %} class="active" {% endif %} href="{{ path('about')}}">A PROPOS</a></li>
            <li><a {% if var is same as ('nos_vehicules') %} class="active" {% endif %} href="{{ path('nos_vehicules')}}">NOS VÉHICULES</a></li>
            <li><a {% if var is same as ('devis') %} class="active" {% endif %} href="{{ path('devis')}}">DEMANDER UN DEVIS </a></li>
            <li><a {% if var is same as ('contact') %} class="active" {% endif %} href="{{ path('contact')}}">CONTACT</a></li>
          </ul>
        <i class="bi bi-list mobile-nav-toggle"></i>
      </nav><!-- .navbar -->

    </div>
  </header>
  {% include "footer.html.twig" %}
{% endblock %}
{% block test %}
  
{% endblock %}