<form method="post" action="{{ path('sectioncarousel_delete', {'id': sectioncarousel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sectioncarousel.id) }}">
    <button class="btn">Delete</button>
</form>
